<h2 class="servicios-title">
        Lo que hacemos
    </h2>
<div class="servicios-container">

  <!-- Lado Izquierdo -->
  <div class="left-side">
    <div class="box-grande"></div>
    <div class="boxes-chicos">
      <div class="box-chico"></div>
      <div class="box-chico"></div>
    </div>
  </div>
  
  <!-- Lado Derecho -->
  <div class="right-side">
    <div class="boxes-arriba">
      <div class="box-derecho"></div>
      <div class="box-derecho"></div>
      <div class="box-derecho"></div>
      <div class="box-derecho"></div>
    </div>
    <div class="box-abajo"></div>
  </div>
</div>

<style>
    .servicios-title{
        display: flex;
        color: var(--color-primary);
        justify-self: center;
        font-size: 2.2rem;
    }
  .servicios-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    height: 100vh;
    padding: 1rem;
  }

  .left-side {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .box-grande {
    flex: 3;
    border: 2px solid #333;
  }

  .boxes-chicos {
    flex: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .box-chico {
    border: 2px solid #333;
  }

  .right-side {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .boxes-arriba {
    flex: 3;
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr 1fr;
    gap: 1rem;
  }

  .box-derecho {
    border: 2px solid #333;
  }

  .box-abajo {
    flex: 1;
    border: 2px solid #333;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .servicios-container {
      grid-template-columns: 1fr;
      height: auto;
      padding: 1rem;
      gap: 1rem;
    }

    .left-side {
      display: contents;
    }

    .right-side {
      display: contents;
    }

    .box-grande {
      order: 1;
      min-height: 300px;
    }

    .boxes-chicos {
      order: 2;
      min-height: 150px;
    }

    .boxes-arriba {
      order: 3;
      min-height: 300px;
    }

    .box-abajo {
      order: 4;
      min-height: 150px;
    }
  }

  @media (max-width: 480px) {
    .servicios-container {
      padding: 0.5rem;
      gap: 0.75rem;
    }

    .box-grande {
      order: 1;
      min-height: 250px;
    }

    .boxes-chicos {
      display: contents;
    }

    .box-chico {
      min-height: 150px;
    }

    .box-chico:nth-child(1) {
      order: 2;
    }

    .box-chico:nth-child(2) {
      order: 3;
    }

    .boxes-arriba {
      grid-template-columns: 1fr;
      grid-template-rows: auto;
      min-height: auto;
    }

    .box-derecho {
      min-height: 150px;
    }

    .box-derecho:nth-child(1) {
      order: 4;
    }

    .box-derecho:nth-child(2) {
      order: 5;
    }

    .box-derecho:nth-child(3) {
      order: 6;
    }

    .box-derecho:nth-child(4) {
      order: 7;
    }

    .box-abajo {
      order: 8;
      min-height: 150px;
    }
  }
</style>